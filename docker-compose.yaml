services:
  db:
    image: postgres
    container_name: db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ./backend/MessengerDBInit.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres_data:/var/lib/postgresql
    ports:
      - ${POSTGRES_PORT}:${POSTGRES_PORT}
    restart: always

  redis:
    image: redis
    container_name: redis
    ports:
      - ${REDIS_PORT}:${REDIS_PORT}
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    restart: always

  minio:
    image: minio/minio
    container_name: minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    restart: always

  backend-service:
    build:
      context: .
      dockerfile: DockerfileBackend
    container_name: backend
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_HOST: db
      POSTGRES_PORT: ${POSTGRES_PORT}
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
      # Собирается из единого источника PUBLIC_SCHEME/PUBLIC_HOST в .env
      FRONTEND_URL: ${PUBLIC_SCHEME}://${PUBLIC_HOST}
      BACKEND_PORT: ${BACKEND_PORT}
      # Адрес отправителя и логин SMTP — из единого ADMIN_EMAIL в .env
      SMTP_FROM: ${ADMIN_EMAIL}
      SMTP_HOSTNAME: ${SMTP_HOSTNAME}
      SMTP_USERNAME: ${ADMIN_EMAIL}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
    expose:
      - "${BACKEND_PORT}"
    depends_on:
      - db
      - redis
      - minio
    restart: always

  frontend-service:
    build:
      context: .
      dockerfile: DockerfileFrontend
      args:
        # Собирается из единого источника PUBLIC_SCHEME/PUBLIC_HOST в .env
        VITE_API_BASE_URL: ${PUBLIC_SCHEME}://${PUBLIC_HOST}
    container_name: frontend
    expose:
      - "${FRONTEND_PORT}"
    environment:
      FRONTEND_STATIC_PORT: ${FRONTEND_PORT}
      VITE_API_BASE_URL: ${PUBLIC_SCHEME}://${PUBLIC_HOST}
    depends_on:
      - backend-service
    restart: always

  caddy:
    image: caddy:2-alpine
    container_name: caddy
    ports:
      - ${CADDY_HTTP_PORT}:80
      - ${CADDY_HTTPS_PORT}:443
    environment:
      # Имя сертификата и контакт Let's Encrypt — из единого источника в .env
      SSL_SERVER_NAME: ${PUBLIC_HOST}
      SSL_CONTACT_EMAIL: ${ADMIN_EMAIL}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend-service
      - backend-service
    restart: always

volumes:
  postgres_data:
  minio_data:
  caddy_data:
  caddy_config:
